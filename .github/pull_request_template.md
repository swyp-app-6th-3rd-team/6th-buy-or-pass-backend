## 해결할 문제

<!-- 이 PR이 없으면 무엇이 안 되는가. 증상이 아니라 원인을 쓴다. -->

## 왜 지금

<!-- 이 시점에 해야 하는 이유. 선행 작업이 무엇을 열어줬는지, 뒤로 미루면 무엇이 막히는지. -->

## 어떻게

<!-- 해결 방식의 핵심. 코드를 나열하지 말고 "왜 이 구조인지"를 쓴다.
     되돌리기 비싼 결정이 있었다면 해당 ADR을 링크한다. -->

## 변경 유형

- [ ] 기능 추가
- [ ] 버그 수정
- [ ] 리팩터링 (동작 변경 없음)
- [ ] 스키마·마이그레이션
- [ ] 문서
- [ ] 빌드·설정

## 한계와 트레이드오프

<!-- 이 선택이 포기한 것. "없음"이면 대개 생각이 덜 된 것이다.
     의도적으로 미룬 것(v1 범위 밖)도 여기에 명시한다. -->

## 검토한 대안

<!-- 무엇을 왜 안 썼는가. 기각 사유가 곧 방어 근거다.
     대안이 정말 없었다면 "왜 없는지"를 쓴다. -->

| 대안 | 기각 사유 |
|---|---|
|  |  |

## Edge Case

<!-- 동시성·타임아웃·재시도·경계값·실패 경로.
     이 템플릿에서 실제로 물렸던 것: 시각 정밀도 불일치(datetime(0) vs 나노초),
     keyset 정렬 키 동률, JSON 왕복 시 타입 소실, 순환 FK. -->

## 아키텍처 규칙

<!-- ArchitectureTest 를 건드렸다면 반드시 적는다. -->

- [ ] 규칙을 추가·수정하지 않았다
- [ ] 규칙을 추가·수정했고, **일부러 위반하는 코드를 넣어 해당 규칙만 실패하는 것을 확인했다**

<!-- 통과만으로는 규칙이 무언가를 지킨다는 증거가 되지 않는다.
     테스트가 초록색인데 보호는 없는 상태가 가장 나쁘다. -->

## 마이그레이션

<!-- 스키마 변경이 있으면 채운다. 없으면 "해당 없음". -->

- 적용 순서:
- 롤백 방법:
- 기존 데이터 영향:

## 리뷰 포인트

<!-- 리뷰어가 어디를 집중해서 봐야 하는지. 자신 없는 부분을 숨기지 말 것. -->

## 테스트

<!-- 무엇을 어떻게 검증했는가. 대리지표(빌드 green·테스트 통과) 금지 —
     대응하는 PRD 완료 판정 항목과 그 검증 방법을 적는다. -->

| PRD 완료 판정 | 검증 방법 | 결과 |
|---|---|---|
|  |  |  |

<!--
해당 없는 섹션은 삭제하지 말고 "해당 없음 + 사유"로 남긴다.
-->
